# Railway Environment Variables Configuration
# Copy these variables to your Railway project environment settings

# Application Environment
NODE_ENV=production
PORT=3000

# Security - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secure-64-character-jwt-secret-key-goes-here-generate-new

# Railway Storage Configuration
RAILWAY_VOLUME_MOUNT_PATH=/app/data

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security Configuration  
BCRYPT_ROUNDS=12

# Timezone
TZ=America/Mexico_City

# CORS Configuration (automatically configured in server.js)
# RAILWAY_PUBLIC_DOMAIN is automatically set by Railway

# Optional: Custom domain if you have one
# CUSTOM_DOMAIN=yourdomain.com